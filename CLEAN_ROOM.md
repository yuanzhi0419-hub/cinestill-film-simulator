# 独立实现声明

凤梨罐头 FILM LAB 由 `yuanzhi0419-hub` 根据本仓库中的产品设计规范独立实现和维护。本项目与 `zhirendashu` 无关。

实施边界：

- 不读取或复制先前项目的源码、页面、测试和内部算法结构。
- 不复用先前项目或来源不明的 LUT、XMP、样片和文案。
- 图像处理算法依据公开数学定义重新设计。
- 内置预设使用原创名称和本项目独立定义的参数。
- 用户导入的 LUT 必须由用户自行确认拥有使用权。

设计规范和实施计划保存在 `docs/`，用于记录独立实现过程。
